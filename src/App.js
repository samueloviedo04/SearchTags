import React, {useState, useEffect, useCallback, useRef} from "react"
import Input from './TagSearch'

// itself. This example is a bit elaborate, to demonstrate what's possible.
const App = () => {
  

  return (
    <>
      <Input></Input>
    </>
  )
}

export default App
